# Daily Plan — TrigunAI  (your living backlog, per block)

> The morning brief shows the **top unchecked `- [ ]` item in each block** as TODAY's task.
> Reorder freely — put what matters today on top. Check `- [x]` when done (or move it to the log).
> Tag each task with `@model:haiku|sonnet|opus` to pick the right (cheapest-that-works) Claude model.
> No tag = the block's default model.
>
> **Model rule (save tokens):** start cheap, escalate only when it struggles.
> haiku = mechanical (uploads, scripts, lookups) · sonnet = the middle (most coding/writing) ·
> opus = hard reasoning only (strategy, curriculum/flow design, tricky debugging, RL reward design).

> **🔀 ROUTINE REFIT 2026-07-23 → STUDENT-FUNNEL TRAFFIC (this week's gate).** Decision (Deepak,
> 07-23): the Acharya-for-Students exam-prep funnel is BUILT & LIVE (lms:v73, acharya.trigunai.com/
> exam-prep — free → 14d → ₹199/mo). The bottleneck is no longer product, it's **traffic + working
> conversion**. So this week's gate = **point real students at the funnel → first signups → first
> ₹199 cleared.** Blocks 1 + 3 both serve the funnel. The Patna teacher field sprint (Rohan/visits)
> is **paused this week** — not killed, just not the gate right now (see prior refit in git history).
> Gate = a **cleared ₹199 UTR**, not a signup. Traffic proof = a **public link** driving to /exam-prep.

## 1 Marketing — DRIVE TRAFFIC TO THE FUNNEL  ⭐ (the gate — get students onto /exam-prep)
- [ ] Ship ≥1 public asset (Short/reel, exam-prep angle: "earn your free access — take the test") w/ CTA → acharya.trigunai.com/exam-prep → `content-daily-engine` @model:sonnet
- [ ] Decide on paid: is the Google Ads student campaign (Phase 4 wired) funded + live? (real-money decision — confirm before any spend) @model:sonnet
- [ ] WhatsApp push: point existing student contacts at `quiz <subject>` + the /exam-prep competition @model:haiku
- [ ] Capture the first funnel numbers: signups today, test-completions, any ₹199 → log with links @model:haiku

## 3 Product/Course — MAKE THE FUNNEL CONVERT  ⭐ (don't drive traffic to a broken funnel)
- [ ] Walk the funnel end-to-end AS A STUDENT: landing → signup → test-gen → result → ₹199 path. Find the drop-off, fix ONE thing → `maintain-trigunai-system` @model:sonnet
- [ ] Verify dynamic test-gen quality on a real exam (JEE/NEET/boards) — the test is the product; a bad test kills conversion @model:sonnet
- [ ] Tighten one conversion point on the /exam-prep landing (headline, proof, or the free→₹199 moment) @model:sonnet

## 2 Acharya Provisioning (kept warm — reuse for whichever motion converts first)
- [ ] Warmest lead's branded Acharya tenant demo-ready (Catalyzers Kota half-built) → `acharya-technology-transfer` @model:sonnet

## — PAUSED this week (do NOT let these displace 1+3) —
- [ ] (paused) Patna field sprint: equip Rohan, build shortlist, Maya pre-calls → `teacher-outreach-engine` @model:sonnet
- [ ] (paused) Robotics: 500-iter retrain + VLM eval on the drone policy @model:opus
- [ ] (paused) FlowArt/VR: push VR app toward Live @model:sonnet
- [ ] (optional) TechScan: 3 items + 1 "so-what for TrigunAI" (agents / education AI) @model:haiku
