# ── Dependencies / build artifacts ─────────────────────────────
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# ── Python envs / caches ───────────────────────────────────────
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# ── Vendored third-party repos (have their own history) ────────
robotics_teleop/robot_targets/unitree_ros/

# ── Large media & model binaries (track in cloud/EC2, not git) ─
*.mp4
*.mov
*.avi
*.glb
*.gltf
*.usd
*.usda
*.usdc
*.usdz
*.fbx
*.obj
*.ktx2
*.bin
*.npz
*.npy
*.pth
*.pt
*.onnx
*.tgz
*.tar.gz
*.zip
*.wav
*.mp3
*.flac
*.hdr
*.exr
*.jpg
*.jpeg

# Large image dumps & data assets (keep small docs images via force-add if needed)
course_assets/
stage_design/
**/hdri/
**/renders/
**/intro_out/
webxr-showcase/public/assets/*.json
**/*_dance_*.json
demo_package/
UsdSkelExamples/
**/textures/

# ── Runtime databases / state (may accumulate PII — never commit) ──
*.db
*.sqlite
*.sqlite3

# ── Secrets / keys ─────────────────────────────────────────────
*.pem
.env
.env.*
**/config.yaml
**/*credentials*
**/*secret*

# ── OS / editor cruft ──────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# ── Auto-generated (regenerated by the scanner) ────────────────
project_hub/WORK_LOG.md

# YouTube OAuth secrets / state (never commit)
youtube_series/client_secret.json
youtube_series/token.json
youtube_series/yt_uploaded.json
youtube_series/token_*.json
marketing/.env
marketing/publish_state.json
marketing/lists/*.csv

# Employee training — private allow-list with real phone numbers (never commit)
employee_training/employees.json
