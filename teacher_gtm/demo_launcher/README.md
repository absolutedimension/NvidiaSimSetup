# Hosted demo launcher
LIVE: https://gurukul.trigunai.com/demo (Caddy static, /etc/caddy/Caddyfile handle /demo -> /var/www/gurukul-demo/demo.html on the Gurukul VM).
To update: edit this file -> scp to VM:/var/www/gurukul-demo/demo.html (sudo) -> live instantly (no reload). Or edit on VM directly.
4 demo course tenants + tokened links baked into the buttons.
